  <div class="container mx-auto px-4 py-4">
    <div class="grid grid-cols-1 md:grid-cols-10 items-center gap-8">
      
      <!-- 左側（ロゴ画像）: 3/10 -->
      <div class="md:col-span-4 flex justify-center">
        <%= image_tag "main.png", alt: "Carebit Logo", class: "w-64 h-auto" %>
      </div>

      <!-- 右側（キャッチコピー + ボタン）: 7/10 -->
      <div class="md:col-span-6 space-y-8 text-center md:text-left">
        <div class="space-y-4">
          <h2 class="text-2xl md:text-3xl font-bold text-logo leading-relaxed">
            毎日ひとつ、自分をいたわる小さな行動<br>
            深呼吸から始めよう、今日の自分を大切に
          </h2>
        </div>

        <div class="flex flex-col sm:flex-row sm:justify-start gap-4">
          <%= link_to (defined?(new_user_registration_path) ? new_user_registration_path : "#"),
                      class: "btn" do %>
            新規登録
          <% end %>

          <%= link_to (defined?(new_user_session_path) ? new_user_session_path : "#"),
                      class: "btn" do %>
            ログイン
          <% end %>
        </div>
      </div>
    </div>
  </div>

